# The Granite multilingual embedding model (Apache-2.0) for search by meaning, bundled in the app
# so it works without importing a file. Downloaded at build time and checked against this SHA-256
# (the same hash ModelCatalog.GRANITE_EMBEDDING pins for imported copies); the app itself never
# needs network access. Set model.sha256 to UNPINNED to build without it.
model.url=https://huggingface.co/litert-community/granite-embedding-311m-multilingual-r2/resolve/main/granite-embedding-311m-r2_wi8fc.litertlm
model.sha256=beb2be205abc766a670522e651be5713cecb4e4c33e5ef5c30f6a710d4226db5
